name: Embedded CI (Template)

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  lint-build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup toolchain
        run: |
          echo "Install project-specific toolchain here"

      - name: Lint
        run: |
          echo "Run clang-format/clang-tidy/cppcheck or project linters"

      - name: Build
        run: |
          echo "Run make/cmake/platformio build commands"

      - name: Test
        run: |
          echo "Run unit/integration tests"
